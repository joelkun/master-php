{% extends 'base.html.twig' %}

{% block title %}Tarea{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>{{task.title}}✅</h2>
    <p class="data-task">
        {{ task.user.name  }} {{ task.user.surname  }} | {{ task.user.createdAt|date('d/m/Y h:m')  }} | {% if task.priority == 'high' %}
                    {{ 'Alta' }}
                {% elseif task.priority == 'medium' %}
                    {{ 'Media' }}
                {% elseif task.priority == 'low' %}
                    {{ 'Baja' }}
                {% endif %}
        | {{ task.hours }}h
    </p>
    <p>{{task.content}}</p>
    

</div>
{% endblock %}
